interface:
  display_name: "Talos Contract First"
  short_description: "Guard contract and boundary changes."
  brand_color: "#0F766E"
  default_prompt: "Use $talos-contract-first to implement a Talos schema, contract, or boundary change without drifting from the source of truth."

policy:
  allow_implicit_invocation: true
