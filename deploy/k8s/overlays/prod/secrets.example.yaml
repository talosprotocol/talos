# Example secrets file for production
# DO NOT COMMIT THIS FILE WITH REAL VALUES
# Use sealed-secrets, external-secrets, or vault in production

apiVersion: v1
kind: Secret
metadata:
  name: talos-secrets
  namespace: talos-system
type: Opaque
stringData:
  database-url: postgresql://user:CHANGEME@postgres.prod.svc:5432/talos
  # Add other secrets as needed
