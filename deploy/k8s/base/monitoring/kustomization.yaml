# NOTE: ServiceMonitors and PrometheusRules require Prometheus Operator CRDs
# To apply monitoring resources, use a dedicated overlay or ensure CRDs are installed

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- servicemonitors.yaml
- prometheusrules.yaml
