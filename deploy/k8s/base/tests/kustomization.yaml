apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: talos-tests

resources:
- namespace.yaml
- sdk-go-integration-job.yaml
- sdk-java-integration-job.yaml
- sdk-rust-integration-job.yaml
- networkpolicy.yaml

commonLabels:
  app.kubernetes.io/part-of: talos
  app.kubernetes.io/managed-by: kustomize
  environment: test
