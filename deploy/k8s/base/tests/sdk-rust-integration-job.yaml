apiVersion: batch/v1
kind: Job
metadata:
  name: sdk-rust-integration-tests
  namespace: talos-tests
  labels:
    app: sdk-rust
    test-type: integration
    app.kubernetes.io/part-of: talos
spec:
  backoffLimit: 2
  template:
    metadata:
      labels:
        app: sdk-rust
        test-type: integration
    spec:
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: test-runner
        # CRITICAL: NEVER use :latest
        # CI must use: ghcr.io/talosprotocol/talos-sdk-rust-tool:sha-<gitsha>
        image: ghcr.io/talosprotocol/talos-sdk-rust-tool:sha-abc123
        imagePullPolicy: IfNotPresent
        command: ["scripts/test.sh", "--integration"]
        env:
        - name: TALOS_GATEWAY_URL
          value: "http://talos-gateway.talos-services.svc.cluster.local:8080"
        - name: TALOS_TEST_MODE
          value: "true"
        - name: RUST_LOG
          value: "debug"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
