apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: talos-api
  namespace: talos-system
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
  - host: talos.local
    http:
      paths:
      - path: /api(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
            service:
              name: talos-ai-gateway
              port:
                number: 8001
      - path: /audit(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: talos-audit-service
            port:
              number: 8002
      - path: /config(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: talos-configuration-service
            port:
              number: 8003
      - path: /mcp(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: talos-mcp-connector
            port:
              number: 8082
