# Moved

This repository submodule has moved.

- New location: check deploy/submodules.json

Deprecation:
- This legacy path exists for one release cycle only and will be removed.
- Run:
  - `git submodule sync --recursive`
  - `git submodule update --init --recursive`
