diff --git a/SECURITY.md b/SECURITY.md
index 7bb21e3..8c06488 100644
--- a/SECURITY.md
+++ b/SECURITY.md
@@ -58,6 +58,10 @@ We consider security research conducted in accordance with this policy to be:
 - Physical attacks
 - DoS attacks without payload
 
+## Compliance
+
+For details on SOC 2 Type II and ISO 27001 controls, see [docs/COMPLIANCE.md](docs/COMPLIANCE.md).
+
 ## Cryptography Notes
 
 Talos uses:
diff --git a/artifacts/coverage/summary.json b/artifacts/coverage/summary.json
deleted file mode 100644
index 94013b7..0000000
--- a/artifacts/coverage/summary.json
+++ /dev/null
@@ -1,175 +0,0 @@
-{
-  "generated_at": "2026-01-22T21:19:29.086863",
-  "repos": [
-    {
-      "repo_id": "talos-core-rs",
-      "status": "pass",
-      "coverage": {
-        "line_pct": 0.45790816326530615,
-        "branch_pct": 0.0,
-        "thresholds": {
-          "line": {
-            "required": 0.45,
-            "actual": 0.45790816326530615,
-            "pass": true
-          },
-          "branch": {
-            "required": 0.0,
-            "actual": 0.0,
-            "pass": true
-          }
-        },
-        "path_thresholds": []
-      },
-      "report_path": "core/artifacts/coverage/coverage.xml",
-      "errors": []
-    },
-    {
-      "repo_id": "sdks-go",
-      "status": "skipped",
-      "coverage": {},
-      "report_path": null,
-      "errors": []
-    },
-    {
-      "repo_id": "sdks-python",
-      "status": "pass",
-      "coverage": {
-        "line_pct": 0.813,
-        "branch_pct": 0.6446,
-        "thresholds": {
-          "line": {
-            "required": 0.8,
-            "actual": 0.813,
-            "pass": true
-          },
-          "branch": {
-            "required": 0.6,
-            "actual": 0.6446,
-            "pass": true
-          }
-        },
-        "path_thresholds": [
-          {
-            "glob": "src/talos_sdk/crypto*",
-            "required": 0.8,
-            "actual": 0.8125,
-            "pass": true
-          }
-        ]
-      },
-      "report_path": "sdks/python/artifacts/coverage/coverage.xml",
-      "errors": []
-    },
-    {
-      "repo_id": "sdks-typescript",
-      "status": "pass",
-      "coverage": {
-        "line_pct": 0.7988,
-        "branch_pct": 0.89,
-        "thresholds": {
-          "line": {
-            "required": 0.75,
-            "actual": 0.7988,
-            "pass": true
-          },
-          "branch": {
-            "required": 0.5,
-            "actual": 0.89,
-            "pass": true
-          }
-        },
-        "path_thresholds": [
-          {
-            "glob": "packages/sdk/src/crypto/**",
-            "required": 0.9,
-            "actual": 0.989010989010989,
-            "pass": true
-          }
-        ]
-      },
-      "report_path": "sdks/typescript/artifacts/coverage/cobertura-coverage.xml",
-      "errors": []
-    },
-    {
-      "repo_id": "sdks-rust",
-      "status": "skipped",
-      "coverage": {},
-      "report_path": null,
-      "errors": []
-    },
-    {
-      "repo_id": "sdks-java",
-      "status": "skipped",
-      "coverage": {},
-      "report_path": null,
-      "errors": []
-    },
-    {
-      "repo_id": "talos-contracts",
-      "status": "pass",
-      "coverage": {
-        "line_pct": 0.9627,
-        "branch_pct": 0.0,
-        "thresholds": {
-          "line": {
-            "required": 0.9,
-            "actual": 0.9627,
-            "pass": true
-          },
-          "branch": {
-            "required": 0.0,
-            "actual": 0.0,
-            "pass": true
-          }
-        },
-        "path_thresholds": [
-          {
-            "glob": "python/talos_contracts/**",
-            "required": 0.9,
-            "actual": 0.9298245614035088,
-            "pass": true
-          }
-        ]
-      },
-      "report_path": "contracts/artifacts/coverage/coverage.xml",
-      "errors": []
-    },
-    {
-      "repo_id": "services-ai-gateway",
-      "status": "pass",
-      "coverage": {
-        "line_pct": 0.2951,
-        "branch_pct": 0.0,
-        "thresholds": {
-          "line": {
-            "required": 0.25,
-            "actual": 0.2951,
-            "pass": true
-          },
-          "branch": {
-            "required": 0.0,
-            "actual": 0.0,
-            "pass": true
-          }
-        },
-        "path_thresholds": [
-          {
-            "glob": "app/domain/budgets/**",
-            "required": 0.2,
-            "actual": 0.22797927461139897,
-            "pass": true
-          },
-          {
-            "glob": "app/domain/secrets/**",
-            "required": 0.4,
-            "actual": 0.47692307692307695,
-            "pass": true
-          }
-        ]
-      },
-      "report_path": "services/ai-gateway/artifacts/coverage/coverage.xml",
-      "errors": []
-    }
-  ]
-}
\ No newline at end of file
diff --git a/deploy/scripts/cleanup_all.sh b/deploy/scripts/cleanup_all.sh
index 3fd79f6..d98aaa0 100755
--- a/deploy/scripts/cleanup_all.sh
+++ b/deploy/scripts/cleanup_all.sh
@@ -111,7 +111,7 @@ echo "  ✓ Root project cleaned"
 # =============================================================================
 # 4. Clean temp files
 # =============================================================================
-info "Cleaning temp files..."
+cleanup_generated_files "$ROOT_DIR"
 rm -f /tmp/talos-*.pid /tmp/talos-*.log 2>/dev/null || true
 
 echo ""
diff --git a/deploy/scripts/common.sh b/deploy/scripts/common.sh
index 5c921c3..c9e3310 100644
--- a/deploy/scripts/common.sh
+++ b/deploy/scripts/common.sh
@@ -105,6 +105,48 @@ wait_for_port() {
     error "Timed out waiting for $name"
     return 1
 }
+
+# Remove generated local artifacts without touching source, secrets, or user config.
+cleanup_generated_files() {
+    local root_dir="${1:-$(pwd)}"
+
+    info "Cleaning generated logs, temp files, and test caches..."
+    (
+        cd "$root_dir" || return 1
+
+        rm -f \
+            .DS_Store \
+            *.log \
+            *_output.log \
+            *_output.txt \
+            *_pid.txt \
+            result_*.json \
+            test_a2a_*.json \
+            test_openai.json \
+            karate.jar \
+            task_manifest.json \
+            talos_config.db \
+            test_*.db-shm \
+            test_*.db-wal \
+            2>/dev/null || true
+
+        rm -f artifacts/karate-*.jar 2>/dev/null || true
+        rm -f api-testing/logs/*.log api-testing/karate/karate.jar api-testing/karate/.cache/karate-*.jar 2>/dev/null || true
+        rm -rf *.egg-info 2>/dev/null || true
+        rmdir api-testing/logs api-testing/karate/.cache 2>/dev/null || true
+
+        find . -name "__pycache__" -type d -prune -exec rm -rf {} + 2>/dev/null || true
+        find . -name ".pytest_cache" -type d -prune -exec rm -rf {} + 2>/dev/null || true
+        find . -name ".ruff_cache" -type d -prune -exec rm -rf {} + 2>/dev/null || true
+        find . -path "*/artifacts/coverage" -type d -prune -exec rm -rf {} + 2>/dev/null || true
+        find . -name "playwright-report" -type d -prune -exec rm -rf {} + 2>/dev/null || true
+        find . -name "test-results" -type d -prune -exec rm -rf {} + 2>/dev/null || true
+        find . -name "*.egg-info" -type d -prune -exec rm -rf {} + 2>/dev/null || true
+        find . -name "*.log" -type f -delete 2>/dev/null || true
+        find . -name "*.tmp" -type f -delete 2>/dev/null || true
+        find . -name "*.pyc" -type f -delete 2>/dev/null || true
+    )
+}
 # Install Dependencies for a Repo
 install_deps() {
     local repo_dir="$1"
diff --git a/docker-compose.yml b/docker-compose.yml
index 2a17713..7a9790f 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -1,5 +1,3 @@
-version: "3.8"
-
 services:
   # PostgreSQL Database
   postgres:
@@ -297,6 +295,8 @@ services:
       - AUTH_TRUST_HOST=true
       - OLLAMA_URL=http://ollama:11434
       - DEV_MODE=true
+      - AUTH_ADMIN_SECRET=${AUTH_ADMIN_SECRET:-dev-admin-secret}
+      - AUTH_ADMIN_PRINCIPAL=${AUTH_ADMIN_PRINCIPAL:-dev-admin}
       - AUTH_COOKIE_HMAC_SECRET=${AUTH_COOKIE_HMAC_SECRET:-REDACTED_SECRET_LEAK_20260421<}
     depends_on:
       - talos-gateway-us # Wait for primary region
@@ -354,6 +354,8 @@ services:
     environment:
       - TALOS_GATEWAY_URL=http://talos-ingress:10000
       - TALOS_AUDIT_URL=http://talos-audit-service:8001
+      - TALOS_TUI_ADMIN_SECRET=${AUTH_ADMIN_SECRET:-dev-admin-secret}
+      - TALOS_TUI_ADMIN_PRINCIPAL=${AUTH_ADMIN_PRINCIPAL:-dev-admin}
     networks:
       - talos-network
     depends_on:
diff --git a/pyproject.toml b/pyproject.toml
index 3a8f3f0..c32bc44 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -51,7 +51,7 @@ include = ["src*", "talos*"]
 
 [tool.pytest.ini_options]
 asyncio_mode = "auto"
-testpaths = ["tests"]
+testpaths = ["tests", "api-testing/pytest"]
 
 [tool.uv.workspace]
 members = [
diff --git a/scripts/bash/test.sh b/scripts/bash/test.sh
index d74ce73..7d1ab5b 100755
--- a/scripts/bash/test.sh
+++ b/scripts/bash/test.sh
@@ -9,15 +9,15 @@ echo "Testing talos (root project)..."
 
 # Lint checks
 echo "Running ruff check..."
-ruff check src tests
+ruff check src tests api-testing/pytest
 
 echo "Running ruff format check..."
-if ! ruff format --check src tests 2>/dev/null; then
-	echo "  ⚠ Some files need formatting (run: ruff format src tests)"
+if ! ruff format --check src tests api-testing/pytest 2>/dev/null; then
+	echo "  ⚠ Some files need formatting (run: ruff format src tests api-testing/pytest)"
 fi
 
 # Unit tests
 echo "Running pytest..."
-pytest tests/ --maxfail=1 -q
+pytest tests/ api-testing/pytest --maxfail=1 -q
 
 echo "talos (root) tests passed."
diff --git a/scripts/run-karate.sh b/scripts/run-karate.sh
old mode 100755
new mode 100644
index 7345547..d35b859
--- a/scripts/run-karate.sh
+++ b/scripts/run-karate.sh
@@ -1,19 +1,8 @@
-#!/bin/bash
-# scripts/run-karate.sh
+#!/usr/bin/env bash
+set -euo pipefail
 
-KARATE_VERSION="1.4.1"
-KARATE_JAR="artifacts/karate-${KARATE_VERSION}.jar"
-TEST_DIR="tests/karate"
+SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
+ROOT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
 
-# Ensure artifacts directory exists
-mkdir -p artifacts
-
-# Download Karate JAR if missing
-if [ ! -f "$KARATE_JAR" ]; then
-  echo "Downloading Karate ${KARATE_VERSION}..."
-  curl -L -o "$KARATE_JAR" "https://github.com/karatelabs/karate/releases/download/v${KARATE_VERSION}/karate-${KARATE_VERSION}.jar"
-fi
-
-# Run Karate tests
-echo "Running Karate tests against http://localhost:8000 (Gateway) and http://localhost:8002 (Audit)..."
-java -jar "$KARATE_JAR" "$TEST_DIR"
+echo "DEPRECATED: scripts/run-karate.sh has moved to api-testing/karate/run-karate.sh" >&2
+exec "$ROOT_DIR/api-testing/karate/run-karate.sh" "$@"
diff --git a/sdks/docker-compose.test.yml b/sdks/docker-compose.test.yml
index 659ccfa..8aeb06f 100644
--- a/sdks/docker-compose.test.yml
+++ b/sdks/docker-compose.test.yml
@@ -69,13 +69,14 @@ services:
       DATABASE_URL: postgresql://talos:talos_test@postgres:5432/talos_test
       REDIS_URL: redis://redis:6379
       ENV: test
+      DEV_MODE: "true"
       LOG_LEVEL: debug
     ports:
       - "8080:8080"
     networks:
       - talos-test
     healthcheck:
-      test: ["CMD", "curl", "-f", "http://localhost:8080/healthz"]
+      test: ["CMD", "python3", "-c", "import http.client; conn = http.client.HTTPConnection('localhost', 8080); conn.request('GET', '/healthz'); res = conn.getresponse(); exit(0 if res.status == 200 else 1)"]
       interval: 10s
       timeout: 3s
       retries: 3
@@ -99,7 +100,7 @@ services:
       TALOS_GATEWAY_URL: http://gateway:8080
       TALOS_TEST_MODE: "true"
     working_dir: /workspace/sdks/go
-    command: ["../../scripts/test.sh", "--integration"]
+    command: ["./scripts/test.sh", "--integration"]
     networks:
       - talos-test
     profiles:
@@ -118,7 +119,7 @@ services:
       TALOS_GATEWAY_URL: http://gateway:8080
       TALOS_TEST_MODE: "true"
     working_dir: /workspace/sdks/java
-    command: ["../../scripts/test.sh", "--integration"]
+    command: ["./scripts/test.sh", "--integration"]
     networks:
       - talos-test
     profiles:
@@ -138,7 +139,7 @@ services:
       TALOS_TEST_MODE: "true"
       RUST_LOG: debug
     working_dir: /workspace/sdks/rust
-    command: ["../../scripts/test.sh", "--integration"]
+    command: ["./scripts/test.sh", "--integration"]
     networks:
       - talos-test
     profiles:
diff --git a/talos/__init__.py b/talos/__init__.py
index 783de76..31f6ca0 100644
--- a/talos/__init__.py
+++ b/talos/__init__.py
@@ -6,14 +6,24 @@ This package provides access to all Talos core, client, and network logic.
 
 __version__ = "0.1.0"
 
+# Core protocol primitives
 from .core.crypto import KeyPair, Wallet
 from .core.session import Session, SessionManager, PrekeyBundle
 from .core.blockchain import Blockchain
 from .core.capability import Capability, CapabilityManager
 
-from .client.client import Client, ClientConfig
+# Network components
 from .network.p2p import P2PNode, P2PConfig
 
+# SDK abstractions (Legacy and current)
+from .config import TalosConfig
+from .identity import Identity
+from .channel import SecureChannel, ChannelPool
+from .legacy_client import TalosClient
+
+# New consolidated client
+from .client.client import Client, ClientConfig
+
 __all__ = [
     "KeyPair",
     "Wallet",
@@ -23,6 +33,11 @@ __all__ = [
     "Blockchain",
     "Capability",
     "CapabilityManager",
+    "TalosConfig",
+    "Identity",
+    "SecureChannel",
+    "ChannelPool",
+    "TalosClient",
     "Client",
     "ClientConfig",
     "P2PNode",
diff --git a/tests/karate/api_validation.feature b/tests/karate/api_validation.feature
deleted file mode 100644
index bd6d5a1..0000000
--- a/tests/karate/api_validation.feature
+++ /dev/null
@@ -1,25 +0,0 @@
-Feature: Backend Capabilities Validation
-
-  Background:
-    * url 'http://localhost:8000'
-    * def chatUrl = 'http://localhost:8090'
-    * def auditUrl = 'http://localhost:8001'
-
-  Scenario: Validate Gateway Base Endpoints
-    Given path '/healthz'
-    When method get
-    Then status 200
-
-  Scenario: Validate Chat Endpoint Echo
-    Given url chatUrl
-    And path '/v1/chat/send'
-    And request { content: "test message", session_id: "test-session" }
-    When method post
-    Then status 200
-    And match response.response != null
-
-  Scenario: Validate Audit Endpoints
-    Given url auditUrl
-    And path '/health'
-    When method get
-    Then status 200
diff --git a/tests/karate/audit.feature b/tests/karate/audit.feature
deleted file mode 100644
index fea456b..0000000
--- a/tests/karate/audit.feature
+++ /dev/null
@@ -1,47 +0,0 @@
-Feature: Audit Service API Tests
-
-Background:
-  * url auditUrl
-  * def eventId = '018f3d6b-7123-7a1b-9c2d-123456789abc'
-
-Scenario: Ingest Audit Event
-  Given path 'api/events/ingest'
-  And request 
-  """
-  {
-    "schema_id": "talos.audit_event",
-    "schema_version": "v1",
-    "event_id": "#(eventId)",
-    "ts": "2026-01-20T12:00:00Z",
-    "request_id": "req-123",
-    "surface_id": "test-surface",
-    "outcome": "OK",
-    "principal": { "id": "tester", "type": "USER" },
-    "http": { "method": "POST", "path": "/test" },
-    "meta": { "test": "data" },
-    "resource": { "type": "test", "id": "res-123" },
-    "event_hash": "66a045b452102c59d840ec097d59d9467e13a3f34f6494e539ffd32c1bb35f18"
-  }
-  """
-  When method post
-  Then status 200 or status 409
-  # 409 if already exists
-
-Scenario: List Audit Events
-  Given path 'api/events'
-  When method get
-  Then status 200
-  And match response.items == '#[]'
-
-Scenario: Get Merkle Proof for Event
-  # Note: This might return 404 if the event was just ingested and not yet anchored, 
-  # but it verifies the endpoint exists and the path works.
-  Given path 'proof', eventId
-  When method get
-  Then status 200 or status 404
-
-Scenario: Get Merkle Root
-  Given path 'root'
-  When method get
-  Then status 200
-  And match response.root_hash == '#string'
diff --git a/tests/karate/gateway.feature b/tests/karate/gateway.feature
deleted file mode 100644
index 326cb98..0000000
--- a/tests/karate/gateway.feature
+++ /dev/null
@@ -1,38 +0,0 @@
-Feature: Gateway API Tests
-
-Background:
-  * url gatewayUrl
-
-Scenario: Verify Gateway Health and Version
-  Given path 'healthz'
-  When method get
-  Then status 200
-  And match response.status == 'ok'
-
-  Given path 'version'
-  When method get
-  Then status 200
-  And match response.service == 'gateway'
-
-Scenario: Verify Gateway Status
-  Given path 'api/gateway/status'
-  When method get
-  Then status 200
-  And match response.state == 'RUNNING'
-  And match response.schema_version == '1'
-
-Scenario: Verify Authorization Rejection (Negative Case)
-  Given path 'mcp/tools/chat'
-  And request { session_id: 'test-session', model: 'gpt-4o', messages: [{ role: 'user', content: 'hello' }] }
-  When method post
-  Then status 403
-  And match response.error == 'Capability verification failed'
-
-Scenario: Verify Authorization with Mock Capability (Positive Case)
-  Given path 'mcp/tools/chat'
-  And request { session_id: 'test-session-auth', model: 'gpt-4o', messages: [{ role: 'user', content: 'hello' }], capability: 'cap_test' }
-  When method post
-  # This might fail if the mock connector is not running, but it validates the auth path
-  Then status 200 or status 500
-  # If 500, it should be a connector error, not an auth error
-  And match response == '#? (_ == null || responseStatus != 500 || response.code == "GATEWAY_ERROR")'
diff --git a/tests/karate/karate-config.js b/tests/karate/karate-config.js
deleted file mode 100644
index 61fb2cd..0000000
--- a/tests/karate/karate-config.js
+++ /dev/null
@@ -1,16 +0,0 @@
-function fn() {
-  var env = karate.env; // get system property 'karate.env'
-  if (!env) {
-    env = 'dev';
-  }
-  var config = {
-    env: env,
-    gatewayUrl: 'http://localhost:8000',
-    auditUrl: 'http://localhost:8002'
-  };
-  
-  karate.configure('connectTimeout', 5000);
-  karate.configure('readTimeout', 5000);
-  
-  return config;
-}
diff --git a/tests/parallel_runner.sh b/tests/parallel_runner.sh
index b1f65ac..734e2f6 100755
--- a/tests/parallel_runner.sh
+++ b/tests/parallel_runner.sh
@@ -8,7 +8,7 @@ echo "Starting Parallelized Test Orchestration"
 
 # Run Karate API Check
 echo "Running Karate API tests..."
-java -jar karate.jar tests/karate/api_validation.feature &
+api-testing/karate/run-karate.sh api_validation.feature &
 KARATE_PID=$!
 
 # Run Playwright Checks (Dockerized)
diff --git a/tests/test_redis_budget.py b/tests/test_redis_budget.py
index f97c9cd..9cbe7cf 100644
--- a/tests/test_redis_budget.py
+++ b/tests/test_redis_budget.py
@@ -94,7 +94,9 @@ class TestRedisBudgetService(unittest.IsolatedAsyncioTestCase):
         self.assertEqual(headers["X-Talos-Budget-Mode"], "warn")
 
     async def test_settle(self):
+        db = MagicMock()
         await self.service.settle(
+            db=db,
             request_id="req1",
             team_id="team1",
             key_id="key1",
